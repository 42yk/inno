services:
  redis:
    image: redis:7-alpine
    container_name: today-menu-redis
    ports:
      - "6380:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  redis-data:
